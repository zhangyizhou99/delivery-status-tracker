# Delivery Status Tracker 开发规则

> 状态：生效中。
>
> 适用范围：本仓库后续所有人工和 AI 参与的代码、测试、配置、数据迁移、文档及演示准备工作。
>
> 相关文档：产品范围和实现方案见 [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md)；执行事实、问题、决策和验证证据见 [PROJECT_LOG.md](PROJECT_LOG.md)。

## 1. 规则语言

- **MUST / 必须**：不可跳过。若确实无法遵守，必须先按第 15 节记录例外决策。
- **MUST NOT / 禁止**：不得执行。
- **SHOULD / 应该**：默认执行；不执行时要在项目日志写明原因。
- **MAY / 可以**：可选，不得影响 MVP 核心交付。

本文件约束“如何开发”，不重新定义产品需求。任何人开始实现前必须先读本文件和开发计划中的当前步骤。

## 2. 资料优先级与冲突处理

发生冲突时，按以下顺序处理：

1. 招聘方原始 brief、邮件和原始 `shipments.csv`。
2. [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) 中标记为 `[REQ-MUST]` 的要求。
3. 开发计划中的 `[DECISION]`、`[ASSUMPTION]`、MVP 合约与完成定义。
4. 本开发规则。
5. [PROJECT_LOG.md](PROJECT_LOG.md) 中后来接受且未被取代的决策。
6. README、代码注释和口头约定。

约束：

- 低优先级文档不得静默覆盖高优先级要求。
- 第 15 节是本规则预先授权的例外机制：已接受的例外决策只能在记录的范围和期限内临时偏离被点名的规则，不能覆盖招聘方要求或开发计划中的 `[REQ-MUST]`。
- 代码行为、测试、README 和上述资料不一致时，视为待解决缺陷，不能选择对自己最方便的一份当答案。
- 如果新信息改变原有理解，必须先记录决策，再同步修改受影响的计划、代码、测试和 README。
- 原始 CSV 必须保留内容语义，不得为了让实现通过而偷偷改样本数据。

## 3. 五条总原则

### 3.1 证据先于结论

- 禁止仅凭代码阅读声称功能“已完成”“已修复”或“可运行”。
- 每个完成声明必须对应自动化命令结果、数据库查询、HTTP 响应或明确的人工验证记录。
- README 只能包含已经逐字执行成功的命令。

### 3.2 一次只推进一个可验证工作单元

- 日志中的工作单元默认对应开发计划中的一个 Step；Step 内的普通小修和定向验证追加在同一 `WORK-###`，避免为每次编辑开卡。
- 若出现跨 Step 的独立缺陷、契约变更或范围变更，再单独创建工作记录或 issue。
- 第一次实质修改后，必须立即执行最窄、最便宜、能够证伪当前假设的验证。
- 验证前不得继续打开相邻功能切片或进行无关重构。

### 3.3 后端是业务规则唯一权威

- 状态转换只在后端一个领域规则模块中定义。
- React 只能消费 API 返回的 `allowed_next_statuses`，禁止复制一份生命周期映射。
- 数据库约束保护状态值，后端事务保护状态转换。

### 3.4 可复现性优先于本机便利

- 主运行路径必须是 Windows PowerShell + PostgreSQL 14–18；README 明确列出 Python、Node 和 PostgreSQL 的支持范围与已验证版本。
- 绝对路径、本机用户名、IDE 隐式环境变量和未记录的手工数据库操作不得成为启动条件。
- “本机已经有数据”不能替代 migration 和 seed。

### 3.5 时间盒和范围是真实约束

- `[MVP-CORE]` 未全部通过前，禁止实现 `[REQ-STRETCH]`。
- 约 3 小时时冻结新功能；4 小时停止编码并记录 what-next。
- 不能以“代码更漂亮”为理由牺牲冷启动、核心测试、README 或 live demo。

## 4. 每个工作单元的强制流程

### 4.1 开始前：建立变更卡

每个开发计划 Step 开始时，必须先在 [PROJECT_LOG.md](PROJECT_LOG.md) 的“工作记录”中建立一个 `WORK-###`。同一 Step 内的小修、失败和复验追加到该记录，不重复开卡。记录至少包含：

- 工作编号：`WORK-###`。
- 对应开发步骤和需求编号，例如 `Step 2 / R1 / R2`。
- 目标行为，而不是文件名堆叠。
- 当前局部假设。
- 最便宜的证伪检查。
- 明确不包含的范围。
- 开始时间。

同时必须：

1. 阅读当前步骤、未解决问题和已接受决策。
2. 检查工作区状态，识别已有改动；禁止覆盖或回退来源不明的修改。
3. 确认依赖的前一步完成闸门已经有证据。
4. 选择最小能够得到反馈的修改，不先全面搭建周边抽象。

纯拼写修正、只读调查、无行为变化的格式化以及当前 Step 内的普通小修不新建工作编号，但仍要挂到当前工作记录并验证相关文件。

### 4.2 实现中：保持修改局部

- 先修改直接控制行为的代码，再修改调用方或表现层。
- 一次工作单元只解决一个根因；发现无关问题时记录为新 issue，不顺手扩大范围。
- 遵循现有结构和命名；没有真实重复或复杂度时不新增抽象层。
- 禁止用临时硬编码、跳过测试、吞异常或放宽类型来换取表面通过。
- 若需求、契约、依赖或架构需要改变，立即暂停代码并执行第 6 节决策流程。

### 4.3 第一次实质修改后：立即验证

验证优先级：

1. 能直接证伪当前行为假设的命令或请求。
2. touched slice 的单元/集成测试。
3. touched slice 的 typecheck、lint、build 或 migration check。
4. 只有没有可执行检查时，才用 diff 和人工检查兜底。

规则：

- 验证通过前禁止继续开发相邻能力。
- 验证失败且仍指向同一根因时，只修当前切片并重跑同一检查。
- 验证推翻假设时，最多向直接控制行为的下一层移动一次，然后重新定义假设。
- 连续两次修复仍失败，必须更新 issue 的根因假设；连续三次仍失败，必须停止盲改，记录 blocker 或做显式降级决策。

### 4.4 工作单元结束：关闭证据链

工作单元只有同时满足以下条件才能标记完成：

- 验收行为真实成立。
- 最窄自动化验证通过。
- 受影响文件没有新增诊断。
- 相关测试没有通过删除断言、降低范围或添加 skip 被“修绿”。
- 问题、决策、验证命令和结果已同步到项目日志。
- 对外契约或运行方式变化时，相关文档已同步。
- 没有把 debug 输出、临时数据、secret 或本地路径留进仓库。

完成一个开发计划步骤时，还必须执行该步骤的完成闸门；不能只因为文件已创建就勾选完成。

## 5. 范围和时间管理规则

### 5.1 范围分级

- **Core**：开发计划中的 `[REQ-MUST]` 和 `[MVP-CORE]`，不可降级。
- **Quality**：`[MVP-QUALITY]`，应完成，但 4 小时时必须诚实转入 what-next。
- **Stretch**：`[REQ-STRETCH]`，Core 全绿且仍有时间才可开始。
- **Out**：`[OUT]`，除非招聘方新增要求，否则禁止实现。

### 5.2 时间记录

- 实际编码计时从 Step 0 开始，在项目日志记录开始、暂停、恢复和停止。
- 等待镜像下载等纯等待时间可以单独标记，但不得伪造实际投入。
- 每完成一个 Step，记录累计时间和剩余风险。
- 到 3 小时时必须做一次范围检查，只允许修复、集成、测试、README 和演示准备。
- 到 4 小时时停止新增改动；未完成内容进入 what-next，写明影响和下一步。

### 5.3 固定降级顺序

需要缩减时按以下顺序停止：

1. 状态历史。
2. 状态筛选。
3. Playwright 和额外前端测试。
4. 排练录屏。
5. 并发自动化测试。
6. 深度视觉与可访问性打磨。

冷启动、CSV 导入、状态规则、列表与更新 API、React 纵向流程、至少两类测试、README 和 AI 使用说明不得被降级。

## 6. 需求、契约和决策变更

### 6.1 必须记录决策的情况

以下任何一项变化都必须在修改代码前创建 `DEC-###`：

- 改变状态语义或转换矩阵。
- 改变 API 路径、请求体、响应字段或 HTTP 状态码。
- 改变数据库 schema、约束、seed 语义或 migration 策略。
- 增删生产依赖、服务或基础设施。
- 改变测试数据库、隔离方式或主启动命令。
- 改变 MVP 范围、完成定义、时间盒或降级顺序。
- 偏离开发计划中已接受的技术选型。
- 接受已知风险、临时 workaround 或延期缺陷。

开发计划已经明确接受的基线决策不必在项目日志重复抄写；只有实施细化、变更或取代时才新增决策。

### 6.2 决策记录最低内容

每个决策必须包含：

- 背景与触发原因。
- 约束和待解决的问题。
- 至少两个真实选项，或解释为什么只有一个可行选项。
- 最终选择及理由。
- 正面效果、代价和剩余风险。
- 受影响的需求、文件、测试和 Demo。
- 验证方式。
- 实施后的实际效果；决定刚接受时可写 `pending`，验证完成后必须回填。
- 状态：`proposed / accepted / superseded / rejected`。

已接受决策发生变化时，禁止删除旧记录；将其标为 `superseded` 并链接新决策。

### 6.3 合约变更的原子性

API 或数据契约变化必须在同一工作单元内同步处理：

1. 后端 schema/实现。
2. 自动化测试。
3. 前端类型与调用。
4. OpenAPI/README 示例。
5. 人工验证步骤。

任何一项遗漏都表示工作单元未完成。

## 7. 问题记录规则

### 7.1 何时必须记录 issue

以下情况必须创建 `ISS-###`：

- 测试、build、migration、冷启动或浏览器流程出现非显然失败。
- 失败改变了我们对根因、架构、环境或需求的理解。
- 发生数据错误、状态覆盖、测试污染、并发异常或无法复现。
- AI 生成或建议的内容被证据证明错误。
- 使用 workaround、留下风险或把问题延期到 what-next。
- 同一个失败经过一次修复仍未解决。
- 问题可能影响评审机器、README 命令或 live demo。

普通拼写错误或立即修正的语法错误可以不单独建 issue；但如果它揭示了可复用的环境/工具陷阱，则应该记录。

### 7.2 Issue 必须基于事实

每个 issue 至少记录：

- 可见症状和原始错误摘要。
- 复现步骤或触发条件。
- 预期与实际行为。
- 影响范围和严重度。
- 调查证据，区分事实与假设。
- 根因；未知时明确写 `unknown`，不得编造。
- 修复或处置。
- 修复后的精确验证命令和结果。
- 是否为 README AI 使用说明候选材料。
- 状态：`open / investigating / resolved / deferred / wont-fix`。

禁止只写“修复了 CORS”“环境有问题”这类无法复盘的描述。

### 7.3 问题关闭条件

Issue 只有满足以下条件才能标为 `resolved`：

- 原始复现步骤已经不再失败。
- 至少一个检查能够防止或发现回归。
- 没有通过隐藏错误、删除验证或手工修改数据库绕过问题。
- 相关决策和文档已同步。

无法在时间盒内修复时标记 `deferred`，写明用户影响、临时措施、风险和下一步，不得假装关闭。

## 8. 后端技术约束

### 8.1 模块职责

- `status_rules.py` 必须是状态转换规则唯一来源，应该保持纯函数或无数据库依赖的领域逻辑。
- Pydantic schema 负责 API 输入输出验证；ORM model 负责持久化映射；router 负责 HTTP 编排。
- 领域规则不得依赖 React，也不应直接返回 HTTP response。
- 不为了形式强制增加 repository/service/interface 多层；只有真实复杂度出现时才通过决策引入。

### 8.2 Python 与错误处理

- 生产代码必须有清楚类型，状态必须使用 enum 或等价受控类型，不在各处散落字符串比较。
- 禁止裸 `except:`、静默吞异常或把所有异常统一转换为 `200`/`500`。
- 预期业务错误必须稳定映射为计划定义的 `404`、`409`、`422`。
- 错误响应不得暴露数据库凭据、堆栈、容器路径或内部 SQL。
- 日志应该帮助定位启动、seed 和请求失败，但不得打印 secret。

### 8.3 事务

- 一次状态更新必须在一个事务内读取、校验、写入和提交。
- 非法转换、请求异常或数据库错误必须回滚，不得留下部分写入。
- 并发保护在实现时应使用 PostgreSQL 行锁并在锁内重新读取状态；如果因时间盒延期，必须记录决策和剩余风险。
- 相同状态 no-op 不更新 `updated_at`，除非先修改既定假设并同步测试与文档。

## 9. PostgreSQL、migration 和 seed 约束

- 主流程和 API 集成测试必须针对 PostgreSQL；禁止用 SQLite 的通过结果代替 PostgreSQL 验证。
- schema 只能通过 Alembic migration 演进；禁止以运行时 `create_all` 作为正式建表方案。
- migration 必须可从空数据库执行，并在提交前人工检查生成 SQL/约束是否符合意图。
- `reference` 必须唯一且非空；`customer_name` 必须非空；`status` 必须有数据库 CHECK 或同等约束。
- 时间字段使用带时区 UTC 语义。
- 应用查询必须参数化或通过 SQLAlchemy 构造；禁止拼接外部输入形成 SQL。

Seed 必须满足：

- 读取仓库中的真实 `data/shipments.csv`，不得把 20 条记录复制进 Python 或 migration。
- MVP 输入固定为该仓库文件；禁止增加任意路径参数、网页上传、导入 API 或通用导入脚本。
- 在一个事务中校验并导入，错误时整体失败并报告行号。
- 对重复 reference 执行 insert-if-missing，禁止覆盖用户已经更新的状态。
- 重启 API 后记录数仍为 20，已更新状态保持不变。
- 只有显式运行 `./scripts/reset.ps1` 时，才恢复 CSV 初始状态。

API 集成测试必须使用独立数据库，例如 `tracker_test`：

- 测试数据库名必须以 `_test` 结尾；测试启动时若检测到目标是 Demo 数据库，必须立即失败。
- 测试库执行同一套 Alembic migration，不维护测试专用 schema 分叉。
- 测试数据使用 `TEST-...` reference，并通过 fixture 清理或事务隔离保证重复运行。
- 禁止自动化测试修改 `TV-1001` 至 `TV-1020` 的 Demo 数据。
- 测试结束后应断言没有遗留 `TEST-%` 数据；Demo 数据库仍应保持 20 条样本记录。

## 10. API 合约约束

MVP 合约以开发计划第 7 节为准：

- `GET /api/shipments` 返回稳定的 `reference ASC` 列表和 `total`。
- `PATCH /api/shipments/{reference}/status` 处理状态更新。
- `GET /api/health` 服务于 PowerShell 启动就绪检查和排障。
- item 必须包含 `reference`、`customer_name`、`status`、`allowed_next_statuses`、`updated_at`。
- 不存在 reference 返回 `404`。
- 未知/缺失/错误类型的 status 返回 `422`。
- 生命周期非法转换返回结构化 `409`，并包含 current、requested 和 allowed 信息。
- 同状态请求返回 `200` no-op。

约束：

- 前端需要的下一状态由 API 派生返回，不由数据库冗余保存。
- 空列表返回 `200` 和空 items，不返回 `404`。
- 禁止为通过 UI 演示而放宽后端规则。
- 任何字段或错误码变化必须执行第 6.3 节完整同步。

## 11. React 技术与交互约束

- 使用 TypeScript，API 类型集中定义；禁止用无边界的 `any` 绕过不匹配。
- 所有 HTTP 调用集中在 API client，不在多个组件散落 URL 和 fetch 解析逻辑。
- 禁止复制状态转换矩阵；操作项只来自 `allowed_next_statuses`。
- 使用服务端确认后的响应更新对应行，不做 optimistic mutation。
- 禁止调用 `window.location.reload()`；更新后必须只替换页面中的目标 shipment。
- 必须实现 loading、error、empty 和 success 状态。
- mutation 期间只禁用当前行；失败时保留旧状态并展示服务端错误。
- `delivered` 和 `failed` 不显示可执行更新控件。
- 列表 key 使用稳定业务标识，不能使用数组下标。
- 状态必须同时用文字表达，不能只靠颜色。
- 基础键盘焦点、label、错误反馈和窄屏不重叠属于质量目标；若延期必须记录。

禁止在 MVP 中引入 Redux、TanStack Query 或大型 UI 库，除非新的复杂度已经被证据证明且有已接受决策。

## 12. Windows 启动、配置和安全约束

- 主启动命令保持 `./scripts/dev.ps1`；变化必须记录决策并同步 README。
- `setup.ps1` 负责项目级依赖和数据库初始化，`dev.ps1` 管理 API/Web 子进程，`test.ps1` 运行质量检查，`reset.ps1` 显式重建 Demo 数据。
- API 启动前必须用 `pg_isready` 等待 PostgreSQL ready，不能依赖一次幸运的启动顺序。
- 脚本必须兼容 Windows PowerShell 5.1，并从脚本位置解析仓库根目录，不依赖调用者当前目录。
- 应用配置通过环境变量注入；仓库只提交 `.env.example`，禁止提交真实 secret。
- CORS 只允许已知 Web origin，禁止用 `*` 掩盖配置问题。
- 端口、数据库 URL 和 API URL 不得硬编码作者机器路径或 IP。
- Python `requirements.txt` 必须对直接依赖和解析后的传递依赖使用 `==` 精确版本，不允许无版本、`>=` 或浮动范围；虚拟环境使用自动发现或显式指定的 Python 3.11–3.14 创建。
- Node 依赖必须提交 `package-lock.json` 并由脚本使用 `npm ci`；禁止只依赖未锁定的本机 `node_modules`。
- 新增依赖前必须说明它解决的具体问题；标准库或现有依赖足够时不加包。
- 禁止提交本机数据库数据、缓存、虚拟环境、`node_modules`、build 输出、录屏和个人文件。
- 本作业明确不实现认证和部署；这不意味着可以忽略输入校验、secret 管理和错误信息边界。

## 13. 测试与验证规则

### 13.1 行为与最低证据

| 修改类型 | 修改后最窄检查 | 步骤完成前证据 |
| --- | --- | --- |
| 状态规则 | 相关参数化单元测试 | 完整转换矩阵测试 |
| API schema/router | 相关 API 测试 | 成功、404、409、422 和数据库值断言 |
| ORM/migration | migration 执行或检查 | 空库 migration + schema 查询 |
| seed | seed 定向测试/运行 | 首次 20 条、二次仍 20 条且不覆盖更新 |
| React 类型/API client | TypeScript 或相关测试 | lint + production build |
| React 交互 | 浏览器定向流程 | 无 reload、持久化、错误和 pending 人工验证 |
| Windows 启动脚本 | PowerShell 语法/前置检查 | `reset.ps1` 后单命令启动与 health |
| README 命令 | 逐字运行该命令 | 新目录/干净数据库复现 |

### 13.2 测试纪律

- 新增或修复业务行为必须同时新增/更新能失败于旧行为的测试。
- 测试必须断言外部行为和必要持久化结果，不能只断言函数“被调用”。
- 非法更新测试必须额外确认数据库值未改变。
- 测试必须可重复运行，且不能依赖执行顺序。
- 禁止为通过而删除有价值断言、使用无解释的 skip、增加任意 sleep 或捕获所有异常。
- flaky test 视为真实问题，必须记录，不得简单重跑到绿色后忽略。
- 第三方网络服务不得成为测试前提；本项目测试只依赖本机 PostgreSQL。

### 13.3 验证证据

验证必须执行，但普通成功命令不单独编号。以下情况才在项目日志创建 `VAL-###`：

- 一个开发 Step 的完成闸门。
- MVP Core、冷启动、Demo 或提交前的关键证据。
- 重要失败的复现与修复验证。
- 支撑关键决策、MUST 例外或 README 声明的证据。

当前 Step 内的普通 lint、诊断、定向测试和小修复验，只在 `WORK-###` 的验证摘要中一句汇总。阶段验证记录包含：

- 精确命令或人工步骤。
- 时间。
- exit code 或明确结果摘要。
- 验证了什么行为。
- 若为非显然失败，对应的 `ISS-###`。

不需要粘贴数百行日志，也不为正常环境安装、拼写修正或每次成功命令创建条目。目标命令只有实际执行成功后才能标为 verified。

## 14. 文档、Git 和 AI 使用约束

### 14.1 文档同步

- [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) 维护范围、方案和完成标准。
- 本文件维护稳定流程规则，不记录每日运行结果。
- [PROJECT_LOG.md](PROJECT_LOG.md) 维护时间、工作、问题、决策和验证事实。
- README 是评审入口，必须简洁、英文、可从干净环境执行。
- 同一事实只保留一个权威来源，其他文档用链接引用，避免复制后漂移。
- 不能把计划中的目标命令描述成已验证；只有项目日志有成功证据后才能写入 README。

### 14.2 Git 与工作区

- 每个工作单元前后检查 diff，避免混入无关格式化或生成物。
- 禁止回退、覆盖或删除来源不明的已有修改。
- 禁止使用破坏性命令隐藏问题，例如 `git reset --hard`。
- AI 不得自行 commit、建分支或 push，除非用户明确要求。
- 若用户选择提交 commit，应该按可验证工作单元组织，不把无关变化塞进同一 commit。
- lock 文件和 migration 属于应提交的源资产；本地 `.env` 和运行数据不属于。

### 14.3 AI 使用

- AI 输出按“未经验证的候选实现”处理，不因生成速度快而降低审查标准。
- AI 参与的代码、测试、配置和命令都必须由实际工具结果验证。
- 项目日志记录 AI 大幅辅助的工作单元，以及被证据证明错误的 AI 建议/实现。
- 不得虚构 AI 错误；候选事件必须包含症状、根因、发现证据、修复和复验。
- 最终 README 清楚区分 AI 辅助生成与人类负责的需求解释、技术取舍和验证。

## 15. 例外和已知风险处理

若确实需要违反一条 MUST，必须在违反前创建决策记录，内容至少包括：

- 被违反的具体规则。
- 无法遵守的客观原因。
- 影响范围和用户/评审风险。
- 采用的最小临时措施。
- 如何验证临时措施没有掩盖核心错误。
- 何时撤销或转入 what-next。

例外决策是规则明确授权的临时机制，不代表项目日志在一般情况下拥有比本规则更高的优先级。例外到期、撤销或被取代后，必须恢复原规则或正式修订本文件。

以下事项不能通过例外降级：

- 招聘方 `[REQ-MUST]`。
- PostgreSQL、FastAPI、React 三项固定技术要求。
- 非法状态转换必须由后端拒绝。
- 至少两个有意义的测试。
- 可运行 Demo、准确 README 和真实 AI 使用说明。

## 16. 阶段闸门

### 16.1 进入下一个开发 Step 前

- [ ] 当前 Step 的完成闸门已有证据。
- [ ] 当前工作记录已结束或明确标为 blocked。
- [ ] 没有未解释的测试/build/运行错误。
- [ ] 新问题和决策已记录。
- [ ] 没有引入计划外范围。

### 16.2 宣布 MVP Core 完成前

- [ ] 开发计划中的所有 `[MVP-CORE]` 已用证据勾选。
- [ ] 从干净数据库单命令启动成功。
- [ ] CSV 初始数据、合法更新、持久化和非法拒绝均验证。
- [ ] 状态规则测试和 API 集成测试通过。
- [ ] README 命令逐字验证。
- [ ] AI 使用记录包含至少一个真实候选错误事件。

### 16.3 提交前

- [ ] 完整质量命令通过，未通过项明确进入 what-next。
- [ ] 从干净目录或解压后的 zip 冷启动成功。
- [ ] Demo 数据已恢复到已知状态。
- [ ] 5 分钟脚本排练通过。
- [ ] Git 中没有 secret、运行数据、缓存、录屏和个人路径。
- [ ] 项目日志与 README 的事实一致。

## 17. 快速执行清单

后续每次开发只需先回答下面七个问题；答不出来就不开始改代码：

1. 这项工作对应哪个 Step 和 requirement？
2. 用户可观察到的目标行为是什么？
3. 当前局部假设是什么？
4. 哪个最便宜检查可以证明假设错了？
5. 第一次编辑后立刻运行什么？
6. 如果发生非显然或有复发风险的失败，记录到哪个 `ISS-###`？
7. 完成后需要更新哪些测试、决策、验证证据和 README？
